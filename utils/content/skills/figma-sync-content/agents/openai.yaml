interface:
  display_name: "Figma Sync Content"
  short_description: "Sync Figma text variables directly into code"
  default_prompt: "Use $figma-sync-content to update mapped project code directly from Figma text variables."

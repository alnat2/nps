interface:
  display_name: "Figma Sync Content"
  short_description: "Import Figma text variables into project JSON"
  default_prompt: "Use $figma-sync-content to sync approved Figma text variables into the project content token file."

interface:
  display_name: "Project Sync Content"
  short_description: "Run project-wide Figma content sync"
  default_prompt: "Use $project-sync-content to update mapped project content directly from Figma."
